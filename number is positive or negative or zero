#include<stdio.h>
#include<math.h>
int main(){
    int a;
    scanf("%d",&a);
    if(a==0){
        printf("%d is a zero",a);
    }
    else
    if(a<0)
    {
        printf("%d is -ve number",a);
    }
    else
    {
        printf("%d is +ve number",a);
    }
}
